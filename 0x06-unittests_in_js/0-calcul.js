/**
 * calculateNumber - Function that calulate sum of two numbers
 * @param {number} a: Arg 1
 * @param {number} b: Arg 2
 * @returns Sum of two numbers
 */

module.exports = function calculateNumber(a, b) {
  return Math.round(a) + Math.round(b);
};
